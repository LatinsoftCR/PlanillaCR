version: '3.8'

services:
  # Servicio Web: Aplicación PHP + Apache con drivers SQL Server
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: planillacr_web
    restart: always
    ports:
      - "8085:80"
    
